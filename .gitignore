# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Replit
.cache/
.local/

# Scratch files (e.g., from Claude Code attachments)
attached_assets/
firstrun-audit.md
scripts/fixtures/.recipe-card-validated
.block4-logs/

# WS9 standing scratch/reference artifacts (kept on disk for CC reference; never
# committed). Named explicitly per WS9 3d Part 3c-2 B6 — no broad globs, so a
# genuinely new artifact still shows in `git status`.
artifacts/api-server/scripts/ws9-block3-inspect-store.cjs
artifacts/api-server/scripts/ws9-block3-top25-A.csv
artifacts/api-server/scripts/ws9-block3-top25-B.csv
artifacts/api-server/scripts/ws9-block3-top25-axis-diff.csv
artifacts/api-server/scripts/ws9-block4b2-rotation-inspect.ts
artifacts/api-server/ws9-block3-catalog-run.json
artifacts/api-server/ws9-block3-resample25.json
artifacts/api-server/ws9-block3-retry3.json
artifacts/api-server/ws9-block3-sample50.json
artifacts/design_tokens_4.ts
artifacts/kiwi_screens_mockup.html
artifacts/kiwi_ux_redesign_spec.md
Claude outputs/

# Spent read-only probe and one-shot measurement scripts (WS9 arc). ONE ignored
# folder rather than 36 explicit names: with that many untracked entries the
# no-broad-globs rule above had already lost the benefit it was protecting,
# because a genuinely new artifact no longer stood out in `git status`. Anything
# OUTSIDE _scratch/ still shows, which is the original intent.
# Probes are written here from the start. Scripts that REVERSE a write stay
# tracked and must never be moved in (retag_revert / restamp_revert / revert_class).
artifacts/api-server/scripts/_scratch/
